trigger:
  - main  # Or the branch you want to trigger the build on

pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'
  docfxVersion: '2.76.0'  # Specify DocFX version if needed

steps:
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '8.x'  # Replace with the specific version you need
    installationPath: $(Agent.ToolsDirectory)/dotnet

- script: |
    Invoke-WebRequest -Uri "https://github.com/dotnet/docfx/archive/refs/tags/v(docfxVersion).zip" -OutFile "docfx.zip"
    Expand-Archive -Path "docfx.zip" -DestinationPath "$(Agent.ToolsDirectory)/docfx"
    $env:PATH += ";$(Agent.ToolsDirectory)/docfx"
  displayName: 'Install DocFX'

- script: |
    dotnet build --configuration $(buildConfiguration)
  displayName: 'Build .NET Project'

- script: |
    $(Agent.ToolsDirectory)/docfx/docfx.exe build --config docfx.json
  displayName: 'Build DocFX Documentation'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: 'docs/_site'
    ArtifactName: 'docs'
  displayName: 'Publish Documentation'
